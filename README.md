# 幻境图谱 (Visionary Scope)

这是一个用于收集和展示 AI 绘画 Prompt 的静态网页画廊。

## ✨ 特性

* **瀑布流布局**：完美展示各种比例的图片。
* **数据分离**：通过 `data.json` 管理图片和 Prompt，无需修改 HTML 代码。
* **深色模式**：支持右上角一键切换深色/浅色主题。
* **即时搜索**：输入关键词即可过滤 Prompt。
* **一键复制**：点击图片查看详情，轻松复制咒语。

## 🚀 如何添加新 Prompt？

1.  **上传图片**：将生成的图片上传到 GitHub 仓库的 `images` 文件夹中（如果你创建了这个文件夹）。
2.  **更新数据**：编辑 `data.json` 文件，在数组末尾添加新条目：

```json
{
  "id": 13,
  "image": "你的图片文件名.jpg", 
  "prompt": "你的咒语内容...",
  "tags": ["分类1", "分类2"]
}
```

## 🖼️ 图片路径说明

目前的 `index.html` 默认配置为使用远程 GitHub 仓库的图片。如果你想使用当前仓库的 `images` 文件夹：

1. 在仓库根目录新建 `images` 文件夹并放入图片。
2. 打开 `index.html`。
3. 找到 `const IMAGE_BASE_URL` 这一行，将其修改为：
   `const IMAGE_BASE_URL = "./images/";`

## 📦 部署

只需将所有文件上传到 GitHub，并在 Settings -> Pages 中开启 GitHub Pages 即可。
